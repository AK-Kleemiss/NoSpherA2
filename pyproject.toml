[project]
name = "NoSphera2"
version = "0.1.0"
description = "Some python dependencies for NoSphera2. Mainly for testing"
requires-python = ">=3.14"
dependencies = [
    "pytest>=9.0.2",
]
[tool.pytest.ini_options]
testpaths = [
    "tests",
]