name: CI MacOS

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:

    runs-on: macos-latest

    steps:
    - uses: actions/checkout@v2
    - name: make_MacOS
      run: brew install libomp && cd Mac && make && cp NoSpherA2 NoSpherA2_Mac